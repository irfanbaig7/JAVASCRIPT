
// we can-not change them
const name = "irfan  baig"

// we can change them
let age = 20

// we can change them Every-Where
var dressColor = "balck"

age = 25
// console.log(age);

// name = "Akash"
// console.log(name); // Showing Error


// Example

const salary = 50000
const bonus = salary > 40000 ? 10000 : 5000
console.log(`Total Compensation : ${salary + bonus}` );
